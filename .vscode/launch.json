{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open Nexus Knowledge Hub (index.html)",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}